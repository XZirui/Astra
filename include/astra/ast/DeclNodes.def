// ReSharper disable once CppMissingIncludeGuard

ABSTRACT_NODE(Decl, ASTNode,
    ATTR(Identifier *, Name)
)

// parameter
NODE(ParamDecl, Decl,
    ATTR(ParamType, ParamKind)
    ATTR(Type *, ParamType)
    ATTR(Expr *, DefaultValue) // optional
)

// functionDecl
NODE(FunctionDecl, Decl,
    ATTR_LIST(ParamDecl *, Params)
    ATTR(Type *, ReturnType)
    ATTR(Stmt *, Body)
)